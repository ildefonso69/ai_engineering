# Landing dashboard: one card per context of the reference stack, mapping the
# Master's journey (transactional estimation → conversation → RAG) to the UI.
# Pure rendering — no FastAPI calls here.
class HomeController < ApplicationController
  def index
  end
end
